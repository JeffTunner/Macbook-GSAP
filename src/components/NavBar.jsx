import React from 'react'

const NavBar = () => {
    return (
        <header>
            <nav>
                <img src={"/logo.svg"} alt={"apple-logo"} />

                <ul>
                    {[]}
                </ul>
            </nav>
        </header>
    );
}
export default NavBar
